# Pins the Rust toolchain so contributors, CI and Docker agree.
# Bump in lockstep with [workspace.package] rust-version.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy", "rust-src"]
profile = "minimal"