---
title: Seed is the Artwork
description: Open-source, self-hostable, deterministic generative-art platform — built in Rust.
template: splash
---

<div style="text-align:center; padding: 3rem 0;">

# 🌱 seed-canvas

**The same seed + template + params always produces the same image —
on every platform, byte for byte.**

```bash
cargo install seed-canvas-cli
seed-canvas render --template galaxy --seed cosmos --out cosmos.png
seed-canvas serve   # → http://127.0.0.1:8080
```

</div>